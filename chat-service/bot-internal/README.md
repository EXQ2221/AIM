# bot-internal

Bot 子域模块，承载 Bot 相关能力。

- `biz/`：Bot 核心业务逻辑（当前已迁入）。
- `handler/`：对外入口适配层（后续逐步沉淀）。
- `repository/`：Bot 子域仓储抽象（预留）。
- `model/`：Bot 子域模型（预留）。
- `config/`：Bot 子域配置（预留）。
- `client/`：Bot 子域外部依赖客户端（预留）。
