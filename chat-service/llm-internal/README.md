# llm-internal

LLM 子域模块，承载多 Provider LLM 调用能力。

- `client/`：LLM 客户端与 Registry（当前已迁入）。
- `biz/`：LLM 业务编排层（预留）。
- `handler/`：对外入口适配层（预留）。
- `repository/`：LLM 子域仓储抽象（预留）。
- `model/`：LLM 子域模型（预留）。
- `config/`：LLM 子域配置（预留）。
