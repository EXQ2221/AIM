# rag-internal

RAG 子域模块，承载文档切分、向量化与检索相关能力。

- `biz/`：RAG 业务逻辑（当前已迁入）。
- `client/`：Embedding 客户端与 Provider 适配（当前已迁入）。
- `handler/`：对外入口适配层（预留）。
- `repository/`：RAG 子域仓储抽象（预留）。
- `model/`：RAG 子域模型（预留）。
- `config/`：RAG 子域配置（预留）。
